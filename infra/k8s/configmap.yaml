apiVersion: v1
kind: ConfigMap
metadata:
  name: emissions-config
  labels:
    app.kubernetes.io/name: emissionsv2
    app.kubernetes.io/part-of: emissions-platform
data:
  NODE_ENV: "production"
  PORT: "4000"
  ACCESS_TOKEN_TTL: "15m"
  REFRESH_TOKEN_TTL_DAYS: "7"
  LOG_LEVEL: "info"
  CORS_ORIGIN: "https://app.emissionsv2.com"
